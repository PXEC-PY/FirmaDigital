namespace ValidadorFirmas.Domain.Enums;

/// <summary>Rol de un usuario autenticado, usado para control de acceso basado en roles (RBAC).</summary>
public enum UserRole
{
    Administrador,
    Auditor,
    Usuario
}
